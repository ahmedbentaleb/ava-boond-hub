AVA MANAGER — SESSION BRAIN CODE, CINQUIÈME TOUR
================================================
24/09/2026. Six points — dont le schéma V1 complet, décidé par Hamada. Le mot d'Ahmed est donné pour ce qui suit.

À lire d'abord : _ops/ARBITRAGE_AUDIT_2026-09-24.md (D-25 → D-29) et
Desktop/ava-audit-5/rapport/CONSTATS.md (V-108, V-111 → V-114).

Ton poste :
  cd /c/Users/Hamada/Desktop/ava-brain
  git fetch "/c/Users/Hamada/Desktop/Ava Manager" lot-2
  git checkout lot-2-brain && git merge FETCH_HEAD
Base ava_brain, refaite à neuf. Chaque commit porte « Role: brain ».


B1  V-108 · D-25   MIGRATION 010 — L'AGENCE RESPONSABLE
    - `societe.agence_responsable_id` et `contact.agence_responsable_id`
      (FK agence), remplies : agence du manager s'il existe, sinon agence
      du compte créateur lu dans `evenement_metier`, sinon agence par
      défaut ; puis **NOT NULL**
    - `profil_candidat.agence_id` : remplissage puis **NOT NULL** (V-114 —
      c'est elle que la reprise Boond remplira)
    - la politique `societe.perimetre.mode` semée à `agence_responsable`
    - `--single-transaction`, rejouable deux fois de suite
    Témoin : base neuve → 0 ligne nulle sur les trois colonnes.
    ⚠️ Grok branche le périmètre dessus de son côté : dis-le dans ta réponse.

B2  V-111 · D-27   UN NUMÉRO = UN FICHIER
    Les deux migrations `009` : la seconde devient `010` (et la tienne
    passe à `011` si besoin — le journal dit le vrai ordre).
    `ava.schema_migrations` porte un **rang unique** (entier) et la date
    d'exécution ; une migration inconnue ou hors rang fait échouer `migrate`.
    Témoin : deux fichiers au même numéro → `make migrate` refuse.

B3  V-112 · D-28   F13 DANS LE CLIQUET
    Nouvelle case 13 : `git merge-base --is-ancestor lot-2-brain HEAD`.
    ⚠️ Dans un clone neuf, la référence peut manquer : la case est alors
    **KO avec son motif** (« référence lot-2-brain absente »), jamais verte
    par défaut. Prévois `git fetch origin lot-2-brain` si l'origine l'a.
    Témoin : sur une branche qui ne descend pas de lot-2-brain → KO.

B4  V-113 · D-29   LES TRAILERS VÉRIFIÉS PAR LA CI
    `.github/workflows/ci.yml` : un pas qui refuse la PR si un commit de la
    branche n'a pas de trailer `Role:`, et si un commit qui touche `test/`
    n'a pas `Role: banc`. Le crochet local reste, mais il ne suffit plus.
    Témoin : un commit d'essai sans trailer → le pas échoue (joue-le à la
    main avec la même commande que la CI).


B5  « TOUT EN V1 » (Hamada, 23-24/09)   MIGRATION 011 — LE SCHÉMA V1 COMPLET
    ⛔ Rien ne se code au-delà tant que le canon n'est pas en base. Source unique :
      _ops/MODELE_DONNEES_AVAMANAGER_V1.md §13 (13.1 → 13.5)
      _ops/REGISTRE_POLITIQUES_v1.md §B (70 référentiels) et §C.bis (194 politiques)
      _ops/cartographie/BOOND_REGLAGES_2026-09-23.md (les VALEURS de chaque liste)
    - les 30 référentiels nouveaux, créés ET semés avec les valeurs de Boond,
      chaque code dans sa catégorie
    - les 21 politiques nouvelles, semées à leur défaut
    - les colonnes du §13.1 et du §13.5 sur les tables existantes
    - les tables nouvelles du §13.2 et du §13.5 (contrat RH, documents suivis,
      langues, certifications, dossier technique, expériences, diplômes,
      avantages, devis, facture, lignes, relances, facture fournisseur, achat,
      paiement, jalon, CA additionnel, domaines et outils de contact)
    - `personne.numero_securite_sociale` CHIFFRÉ, lu sous la permission
      `LireDonneesRHSensibles`
    ⛔ `TEXT`, jamais `VARCHAR(n)` ; chaque montant a sa `devise_code` ;
       aucune colonne convertie ; le CA pondéré et le CJM contrat sont des VUES.
    Témoin : `select count(*) from politique` = le compte du registre §E ;
    `\dt ava.ref_*` = 70 ; chaque valeur = son défaut.

B6  LES MURS M-16 → M-18
    Écrits au modèle métier le 24/09 : facture émise immuable (avoir seulement),
    contrats RH sans chevauchement (EXCLUDE), numérotation des factures sans trou
    ni réemploi. Une assertion par mur dans `_ops/SPEC_ASSERTIONS_L7.sql`, puis
    copie exacte dans `test/`. Le plancher se recompte tout seul.
    Témoin : chaque interdit tenté → refus ; trigger désactivé → l'assertion lève.

Ce que tu ne fais jamais : server/ web/ (Grok) ; réécrire une migration
publiée ; toucher « ava », le port 3000, le dossier « Ava Manager » ;
fusionner ou pousser ; assouplir un contrôle qui gêne.

Tu rends : une ligne par point dans `_ops/JOURNAL_BUGS.md` (V-xxx · commit ·
témoin ROUGE → VERT), puis 15 lignes au plus, avec la phrase exacte pour
Grok : « lot-2-brain est prêt : git merge lot-2-brain. »
