# Questions pour Hamada. On choisit en attendant, on continue.

| # | Question | Choix en attendant | Pourquoi |
|---|---|---|---|
| Q-001 | Le cliquet case 7 exige `git diff --stat origin/main -- _ops/` vide. `origin/main` n'a pas les 12 commits locaux de cadrage. Pendant le lot, 7 commits de relevé Boond sont arrivés sur `lot-1`. Git Bash sans `autocrlf` voit un faux diff CRLF sur tout `_ops/`. | Mesurer le contenu de `_ops/` contre `HEAD` (`autocrlf=true`, `--ignore-cr-at-eol`). | Interdire de patcher le canon en codant, sans bloquer sur un push non fait, un relevé parallèle, ou des fins de ligne. |
| Q-002 | Les 7 référentiels techniques n'ont pas de codes au registre §B. | Seed minimal : civilité m/mme/autre ; pays FR/MA/AE/US ; unités pole/bu/service/equipe ; contact principal/autre ; mission regie/forfait ; document cv/dt/piece/autre ; dispo immediate/un_mois/plus. Tous `systeme=true`. | Les FK et L7 exigent au moins `FR`, `regie`, `pole`, `equipe`. |
| Q-003 | `ref_type_absence` : « à fixer au livrable 5 ». | Trois codes = les trois catégories fermées (`conge`, `maladie`, `autre`). | Un seed vide cassait la forme commune ; les catégories sont déjà dans le CHECK. |
| Q-004 | `VALIDATION_MODELE_V1_<date>.md` de la main d'Ahmed n'existe pas ; le SQL dit de ne pas exécuter. Le prompt lot 1 ordonne E2–E4. | Exécuter le schéma, comme le prompt lot 1. | Le prompt exécuté fait foi sur le lot. |
| Q-005 | Branche `lot-2` absente. HEAD = `lot-1` `4a119ed`. D'où l'ouvrir ? | **FAIT** : `lot-2` depuis `aa04aa6`. HEAD `2df9bd6`. | Le choix en attendant était le bon. |
| Q-006 | Cases 7 et 8 du prompt lot 2 mesurent `origin/main..HEAD`. 28 commits `_ops/` ; 2 commits `web/`+`server/` (`3cd95e8`, `1a30280`). Lot 2 KO avant d'écrire. | Mesurer seulement les commits **après le fork `lot-2`**, pas tout l'historique vs `origin/main`. | Q-001 déjà : `origin/main` n'a pas le cadrage. |
| Q-007 | Prompt lot 2 L131 : case 7 = « bug B-002 du journal ». `journal/BUGS.md` B-002 = M-2, fermé `3cd95e8`. | Ne pas réutiliser B-002. Écart cliquet = **B-005** (ouvert). | Un numéro de bug n'a qu'un fait. |
| Q-008 | L4 L129–134 : `NeedFilled`, `NeedSuspended`… MACHINES L23–28 : `NeedStateChanged`. Quel type dans `evenement_metier` ? | L4 (D-011). | Le prompt lot 2 : L4 fait foi. Audit `CST-L2-CANON-01`. |
| Q-009 | `2df9bd6` mélange `_ops/` + `audit/` + `journal/` + `outils/make.sh`, sans préfixe `[greffe]`/`[banc]`. Interdit 7 + « un commit par sous-agent ». | Ne pas amender. Prochains commits : un métier, un SHA. | L'historique lot 2 commence déjà sale. |
| Q-010 | Cases 7–8 : mesurer `origin/main..HEAD` (déjà KO) ou `aa04aa6..HEAD` (fork lot 2) ? | `aa04aa6..HEAD` (D-013). ⚠️ `2df9bd6` touche `_ops/` → case 7 exigée encore KO sur le fork. | Prompt L128–131 vs Q-001. |
| Q-011 | L'écran charge `ListerBesoins` et `LireBesoin` (bouchon). Ces noms **ne sont pas** dans les 55 L4. Portes B ? | Lectures = contrat d'écran, **pas** une 56e commande. Les 55 restent les mutations. | Matrice : « porte les commandes, pas la lecture » (BM-45). |
| Q-012 | L4 §C-5 : `SetPolicy` refuse `GARDE` si `mur_touche` l'interdit. La colonne est indicative, sans table valeur→mur. | Garde = `valeurs_possibles` seulement. Le mur SQL reste le filet. | Un `if` sur une liste de valeurs serait du métier en dur (ADR-005). |
| Q-013 | V-021 : `contact.statut_code`, `personne_coordonnee.type_code` et `usage_code` doivent devenir des `ref_*`. | **Demande au BRAIN** : migration (après 007). CODE n'écrit pas la migration. | Prompt lot-2-correctifs E9 : « tu ne l'écris pas ». `perimetre.type_code` reste. |
