# Contestation de l'arbitrage du BRAIN — second audit, commit `7e3f78c`

**23 éléments examinés · 15 acceptés · 8 contestés.** L'arbitrage lu est `_ops/ARBITRAGE_AUDIT_2026-09-21.md`
(ajouté par `6503168`, le 22/09 à 10h34). Base de mesure : `ava_audit2_c`, migrations 001 → 007 jouées
une par une en `--single-transaction`, toutes passées. Serveur de sonde : port 3203, `AVA_MODE=prod`,
arrêté à la fin.

⭐ **La contestation qui compte : D-2.** Hors mode banc, la « session » que le serveur accepte est
**l'identifiant du compte**, et les identifiants du seed se suivent. Le compte ADM s'obtient en
10 essais au plus. L'en-tête `x-ava-groupe` a bien été fermé, mais la porte qui le remplace
protège autant que lui.

## Tableau

| # | Élément de l'arbitrage | Verdict | Motif (acceptation) ou preuve (contestation) |
|---|---|---|---|
| 1 | **Compte** « 42 retenus · 5 requalifiés ou partiels » | ⛔ **Contesté** | Le tableau des 47 marque 🔁 **six** constats : 003, 006, 016, 021, 022, 040 (`ARBITRAGE:47,52,61,66,69,81`). 47 − 6 = **41 retenus · 6 requalifiés**. Le compte de l'arbitrage dépasse 47. |
| 2 | V-003 requalifié BRAIN → CODE (D-1) | ✅ Accepté | `DECISIONS_TECHNIQUES_v1.md:28` : T2 a bien été tranché le 18/09, et aucune ligne d'`ETAT_PROJET` d'avant le 22/09 ne lui donne un lot. La **fermeture** est contestée plus bas (D-2). |
| 3 | V-006 requalifié (la garde `mur_touche` tombe) | ✅ Accepté | Mesuré : `SetPolicy` passe (`ok:true`, `rapport/preuves/canon/J_session_uuid.txt`). ⚠️ Reste un défaut : la « liste des commandes affectées » est calculée (`admin.ts:95`) puis **jetée** par `sortieDe` (`executer.ts:42-46` : `politique` fait partie de `WRAP`, donc la sortie devient le seul objet `politique`). L4 §C-5 « SORTIE … et la LISTE des commandes » n'est pas tenu → constat candidat J-12. |
| 4 | V-016 requalifié (D-8 : la commande ne crée plus rien) | ✅ Accepté | `grep "INSERT INTO ref_"` dans `server/src` : une seule écriture de référentiel, `admin.ts:132` (`ManageRefs`). `ExitCandidate` passe par `codeCategorie`, qui répond `GARDE` s'il n'existe aucun code actif (`cycle.ts:107`). |
| 5 | V-021 partiel (D-9 : 3 listes sur 4) | ✅ Accepté | La migration 007, lignes 45-120, crée 3 `ref_*` et des FK qui remplacent les CHECK. `ManageRefs` sur `ref_statut_contact` passe (`J_D9_manage_refs.txt`). ⚠️ Deux restes dans le canon : J-3 (SPEC:658 garde le CHECK) et J-7 (B5 ne voit pas ce type de CHECK). |
| 6 | V-022 requalifié « poste de dev », moyenne | ⛔ **Contesté** | La gravité se défend, pas la condition qui la justifie. « `scram-sha-256` obligatoire sur le VPS » n'est écrit **que** dans l'arbitrage (`ARBITRAGE:69`). T3 (`DECISIONS_TECHNIQUES_v1.md:29`) n'en dit rien, et aucune porte ni aucun cliquet ne le vérifie. Mesure : `pg_hba_file_rules` compte **6 règles `trust`**, réplication comprise, avec `listen_addresses=*`. `ava_serveur` n'a pas de mot de passe (`rolpassword` nul). L'URL par défaut du serveur n'en porte pas non plus (`index.ts:13`). Preuve : `rapport/preuves/canon/J_pg_hba.txt`. |
| 7 | V-040 partiel : « un trailer `Role:` par commit suffit ; crochet posé par script » | ⛔ **Contesté** | (a) `git log 3b59ca5..7e3f78c` : **44 commits sur 52** portent un `Role:`. **8 n'en ont pas** : `1da3dd6 a16f6bc c9b3067 aa4da72 5f9427b 2cf7300 19237f6 afc1683` — dont la migration 007 elle-même. (b) Aucun script ne pose `core.hooksPath` : `git grep hooksPath` ne le trouve que dans la grille et les anciens prompts. Dans le clone neuf, `git config core.hooksPath` est **vide**, donc le crochet ne tourne pas, et le cliquet ne vérifie pas F7. (c) Même avec le trailer, la règle D5 échoue : `test/` a été écrit par `banc` (18 commits), `greffe` (5), `ecran` (1) et un commit sans rôle. |
| 8 | D-1 | ✅ Accepté | Voir ligne 2 : une décision prise mais confiée à personne. |
| 9 | **D-2** : lot 2c, `x-ava-groupe` seulement en banc | ⛔ **Contesté — critique** | `index.ts:143-156` refuse bien `x-ava-groupe` hors banc (401 mesuré). Mais il accepte hors banc `x-ava-session`, et `executer.ts:72-82` (`compteDeSession`) compare `c.id::text = $1` : **la session est l'UUID du compte**. Ce n'est pas un secret. (1) Les UUID du seed se suivent : `005_droits_lot2.sql:185-193`, de `…000000000101` à `…109`. (2) Ils sortent dans les réponses : la sortie de `CreateCompany` pour IA porte `manager_compte_id`, et les UUID sont écrits en 36 colonnes `*_compte_id`. (3) Sonde : `x-ava-session: 00000000-0000-4000-8000-000000000108` → `SetPolicy ok:true`, **ADM trouvé au 9e essai**. (4) Le message d'erreur sert d'oracle : « session inconnue » pour un UUID qui n'existe pas, « permission absente » pour un compte réel. (5) Les refus « session inconnue » ne sont **jamais tracés** : `tracerRefus` insère `''` dans une colonne uuid (erreur 22P02). Après l'énumération, `tentative_refusee` compte 0 ligne « session inconnue ». Preuves : `J_session_uuid.txt`, `J_trace_refus_session_inconnue.txt`. Pendant ce temps, `ETAT_PROJET.md:30` affiche le lot 2c ⬜ : le code livre déjà une authentification que le canon ne décrit pas. |
| 10 | D-3 : `mur_touche` indicatif, L4 §C-5 corrigé | ✅ Accepté | `SPEC_COMMANDES_L4.md:177` et `:278` sont corrigés. En base, `count(mur_touche)` vaut **0 sur 173**. Aucune garde ne le lit (`admin.ts` le renvoie seulement). |
| 11 | D-4 : noms d'événements, L4 gagne, MACHINES se corrige | ⛔ **Contesté (partiel)** | MACHINES est corrigé partout sauf à un endroit. `MACHINES_ETAT_V1.md:23` cite `NeedStateChanged` avec la mention « *D-4 : L4 fait foi* », alors que `grep -c NeedStateChanged _ops/SPEC_COMMANDES_L4.md` rend **0**. Le code l'émet (`besoin.ts:24`, avec `{ etat: "staffing" }` écrit en dur). L'événement vit dans le code et dans MACHINES, jamais dans L4 : la règle « L4 gagne » n'a donc rien à départager. |
| 12 | D-5 : acteurs, la MATRICE gagne, MACHINES se corrige | ⛔ **Contesté (partiel)** | Les deux lignes citées par V-029 (5) sont corrigées (`MACHINES:53`, `:103`). Il en reste une : `MACHINES_ETAT_V1.md:91`, `CancelPrestation`, acteur « **DP** ». La MATRICE (`:132`) et la base (`groupe_permission_perimetre`) donnent **DP et STAF**. |
| 13 | D-6 : doublons `avertir · bloquer · ignorer`, L4 corrigé | ✅ Accepté | `L4:79` et `:100` disent `bloquer`. En base, les trois `doublon.*.mode` ont `["avertir","bloquer","ignorer"]`. Le code compare à `"bloquer"` (`crm.ts:45,286`). Le doublon du registre a disparu (`REGISTRE:152,154`). |
| 14 | D-7 : « SUP ne mute rien » précisé | ✅ Accepté | `MATRICE_DROITS_v1.md:157` : 47 lignes sur 48 vides, la 48e est `SetOwnTheme`. En base, SUP n'a que `SetOwnTheme`. |
| 15 | D-8 : `sorti` existe au seed, MACHINES se corrige | ⛔ **Contesté (mineur)** | La correction crée un écart neuf. L'en-tête de `MACHINES_ETAT_V1.md:32` range `sorti` parmi les « **codes système** ». En base, `ref_etat_candidat.sorti` a **`systeme = false`** (`005_droits_lot2.sql:18-20`) : l'admin peut le désactiver, et `ExitCandidate` tombe alors en `GARDE`. La base a raison : MACHINES doit dire « code semé, non système ». |
| 16 | D-9 : `perimetre.type_code` reste un CHECK, 3 listes → `ref_*` | ✅ Accepté | Motif écrit à `REGISTRE:74` et à `007:37`. Les 3 tables existent, avec FK, la garde `ref_garde` et des GRANT sans DELETE. `ManageRefs` les administre. Aucune commande n'écrit `contact.statut_code` ni `personne_coordonnee` : le code n'a rien à lire. ⚠️ Le commentaire `007:43-44` (« c'est sur elle que le code raisonne ») ne décrit aucun code : `grep` rend 0. |
| 17 | ⛔ « Pas de fusion de `lot-2` sur `main` tant que… » | ✅ Accepté | `git merge-base --is-ancestor 7e3f78c origin/main` → **non**. `origin/main..7e3f78c` = 64 commits. La fusion va dans l'autre sens (`da85e91` fait entrer `main` dans `lot-2`), ce que la règle permet. `_ops/` est identique sur les deux (`git diff --stat 7e3f78c origin/main -- _ops` est vide). |
| 18 | Point 1 : 007, M-15, `ava_serveur`, `tentative_refusee` | ✅ Accepté | Mesuré en base : `ava_lecture_agregats` = exactement 7 relations en SELECT. `ava_serveur` a `LOGIN NOSUPERUSER` et hérite de `ava_app`. Aucun DELETE ni TRUNCATE pour lui. INSERT et SELECT sur `tentative_refusee`. Le serveur refuse de démarrer en superutilisateur (`index.ts`, fin). |
| 19 | Point 2 : une assertion par privilège, `test/` = `_ops/` | ✅ Accepté | `diff -q test/SPEC_ASSERTIONS_L7.sql _ops/…` : identiques. Plancher **29** = 29 lignes `OK   M-` à l'exécution (`J_assertions_L7.txt`, rc 0). M-6 : 3, M-7 : 5, M-8 : 2 (DELETE et TRUNCATE), M-15 : 3. |
| 20 | Point 3 : cliquet case 1 `make_rc = 0`, cases 3 et 9, `fetch-depth: 0` | ✅ Accepté (lu, pas exécuté) | `cliquet.sh:50,96` ; case 3 à `:117`, case 9 à `:207` ; `ci.yml:16`. Le cliquet n'a pas tourné : voir les angles morts. |
| 21 | Point 4 : « les 6 contradictions de V-029 tranchées ✅ » | ⛔ **Contesté** | Tranchées sur le principe, pas partout dans le texte : (1) partiel (ligne 11), (5) partiel (ligne 12). La correction de (6) a laissé un compte faux dans la même cellule du registre (`REGISTRE:332` : 79 + 2 + 86 = **167**, contre 173 annoncé à `:329`) — voir `CANON.md` J-1. |
| 22 | Point 5 : l'authentification inscrite au lot 2c | ✅ Accepté (inscription) | `ETAT_PROJET.md:30`. Ce qui est contesté, c'est l'état réel du code : voir D-2. |
| 23 | Point 7 : ⬜ « Grok corrige, puis même audit rejoué » | ✅ Accepté | L'audit rejoué, c'est celui-ci : le ⬜ tient. Mais la moitié « Grok corrige » était déjà faite quand l'arbitrage a été commité : 37 V- ont un commit, le dernier (`1231bc8`) date du 21/09 23h30, **avant** `6503168`. Voir `CANON.md` J-9 pour `ETAT_PROJET`. |

## Les 6 contradictions de V-029 — état au commit `7e3f78c`

| # | Contradiction | Tranchée ? | Preuve |
|---|---|---|---|
| 1 | Noms d'événements MACHINES / L4 | ⚠️ **Partiel** | Tous les noms sont passés à L4 (`MACHINES:24-28,68-72,89`), sauf `NeedStateChanged` (`MACHINES:23`), absent de L4 |
| 2 | L4 « refus » / registre `bloquer` | ✅ Oui | `L4:79,100` |
| 3 | `mur_touche` garde / indicatif | ✅ Oui | `L4:177,278` ; base 0/173 |
| 4 | « SUP ne mute rien » / `SetOwnTheme` | ✅ Oui | `MATRICE:157` |
| 5 | Acteurs MACHINES / MATRICE | ⚠️ **Partiel** | Les 2 lignes citées sont corrigées ; `MACHINES:91` `CancelPrestation` reste « DP » (base : DP, STAF) |
| 6 | Doublon `doublon.contact.mode` ; commande de comptage 202 | ✅ Oui, avec un reste | Doublon retiré. La commande `awk` de `REGISTRE` rend 172 + 1 en prose = 173 = la base. Mais la même cellule annonce 79 + 2 + 86 = 167 (J-1) |
