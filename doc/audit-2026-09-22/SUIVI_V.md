# Suivi des 47 constats V- du premier audit — commit `7e3f78c`

Second audit, auditeur A. Clone de lecture `ava-audit-2-lecture` sur `7e3f78c` (`git status` vide à la fin).
Base `ava_audit2_a` (001 → 007, reconstruite à neuf pour chaque mutation), serveur sur le port 3201.
Mutations jouées sur une **copie jetable** du clone (scratchpad), jamais sur le clone de lecture.
Sorties longues : `rapport/preuves/suivi/` · scripts : `rapport/preuves/suivi/scripts/` ·
mutations : `rapport/preuves/suivi/mutations/_resume.txt` et `_resume_ecran.txt`.

Portes de contrat rejouées contre 3201 sur base neuve : sante 1/1 · inventaire 1/1 · commandes 56/56 ·
chemin 56/56 · matrice 3/3 · politiques 5/5 (`_contrat_*.txt`). `correctifs.test.ts` **n'a pas été lancé**
(interdit : il démarre des serveurs sur la base « ava ») : ses portes P-066 → P-156 ont été remplacées
par des sondes directes.

| V | Gravité (1er audit) | Verdict | Preuve (commande + sortie courte, ou fichier:ligne) |
|---|---|---|---|
| V-001 | critique | **Fermé** | `007_correctifs_audit.sql` §4 `REVOKE ALL … GRANT` 7 relations. `has_table_privilege('ava_lecture_agregats',…)` → exactement `politique, ref_devise, ref_pays` + 4 `v_*_par_devise` ; `SET ROLE ava_lecture_agregats; select … v_conditions_du_jour` → « droit refusé ». `cmp _ops/SPEC_ASSERTIONS_L7.sql test/SPEC_ASSERTIONS_L7.sql` → identiques ; cliquet case 2 exige l'égalité (`outils/cliquet.sh:109`). `suivi/V-001.txt` |
| V-002 | critique | **Fermé** | `server/src/index.ts:10-13` défaut `ava_serveur` ; `:170-178` refus superutilisateur. `DATABASE_URL=postgres://postgres@…/ava_audit2_a npx tsx src/index.ts` → « refuse de démarrer : current_user=postgres est superutilisateur », exit=1. `ava_serveur` : rolsuper=f, LOGIN, membre de `ava_app`. `suivi/V-002.txt` |
| V-003 | critique | **Ouvert** | L'en-tête `x-ava-groupe` est refusé hors banc (401), mais la « session » est l'UUID du compte en clair : `executer.ts:76` `WHERE c.id::text = $1`. Serveur SANS `AVA_MODE` : `curl -H 'x-ava-session: 00000000-0000-4000-8000-000000000108' …/SetPolicy` → HTTP 200 `ok:true PolicyChanged`, relu en base `doublon.societe.mode = bloquer`, auteur = compte ADM. Les UUID du seed sont fixes et publics (`test/contrat/db.ts:14`). `suivi/V-003.txt` |
| V-004 | critique | **Partiel** | 15 commandes sur des objets de l'agence CAS par des comptes PAR → DROIT (Update/SetNeedPriority/Suspend/Close/DeclareNeedFilled/PositionResource/UpdateProject/CreatePrestation/Sign/Cancel/UpdateCandidate/UpdateResource/SetResourceState/CloseProject/RecordAbsence). **Passent encore** : IA `WithdrawPositioning` (positionnement d'un besoin CAS) → `ok:true` ; IA `CreateAction` sur besoin CAS, DP sur projet CAS, RH sur ressource CAS → `ok:true` ; IA `UploadDocument` sur projet CAS, RH sur ressource et candidat CAS → `ok:true`. `admin.ts:27-45` et `identite.ts:322-373` n'appellent jamais `exigeObjet`. `suivi/V-004.txt` |
| V-005 | critique | **Fermé** | Sonde : DP `RecordTimesheet jour 2026-10-02` dans [2026-10-01, 2026-12-31] → `ok:true TimesheetRecorded` ; `2027-02-01` → GARDE. `suivi/_sonde1.txt` §3 |
| V-006 | critique | **Partiel** | Succès : `SetPolicy` → `ok:true PolicyChanged`, valeur relue (`suivi/V-003.txt`, `_sonde1.txt` §4). Garde `mur_touche` retirée par D-3 (canon corrigé, L4:177). **Reste** : `commandes_affectees` est calculé (`admin.ts:93-96`) mais `sortieDe()` (`executer.ts:37-50`) ne garde que l'objet `politique` : la sortie rendue est `{id,cle,valeur,ancienne,mur_touche}`, sans liste (`_sonde4.txt` fin). |
| V-007 | critique | **Fermé** | `kernel.ts:137-155` `RETURNING id` poussé dans `ctx.evenementIds` ; `executer.ts:85-100` relit par id. Mutation S2 (emit n'écrit plus) → 66 portes tombent (`mutations/_resume.txt`). |
| V-008 | critique | **Fermé** | `cliquet.sh:96` exige `make_rc=0` ET chaque porte ✅ exécutée. Cliquet rejoué avec le journal « serveur mort » du 1er audit : case 1 **KO** avec rc=1, **KO** avec rc forcé à 0 (`non_jouées: P-002 …`), **KO** journal vide. `suivi/V-008.txt` |
| V-009 | critique | **Fermé** | Numéro par numéro (`cliquet.sh:135-145`, `:209-222`) ; CI `fetch-depth: 0` + `main` local (`ci.yml:14-23`). Retrait P-002..P-005 → case 3 KO « disparues : P-002 P-003 P-004 P-005 » ; P-002 ✅→⏳ → case 9 KO. ⚠️ voir candidat N-4. `suivi/V-009.txt` |
| V-010 | critique | **Fermé** | `chemin.test.ts` : une porte de succès par commande (P-071 → P-125), base relue + événement. Mutation S1 (tout INTROUVABLE) : 79 tombent, dont les 55 du chemin ; les 41 restées vertes sont les anciens refus. `mutations/S1_tout_introuvable.txt` |
| V-011 | critique | **Partiel** | Tombent désormais : droit DeclareNeedFilled (P-127), O-2 (P-126), périmètre besoin (P-128), doublon.societe.mode (P-130), ui.theme (P-131), besoin.contact (P-132). **Survivent 120/120** : règle S de `RecordTimesheet` retirée (S4b), `temps.plafond_jour` en dur (S5d), `positionnement.unicite` en dur (S5e) — les trois cas nommés au 1er audit. P-133 ne joue jamais `RecordTimesheet` : il règle la politique et la relit (`politiques.test.ts:74-87`). `mutations/_resume.txt` |
| V-012 | critique | **Fermé** | 29 assertions (plancher compté = 29). GRANT UPDATE evenement_metier → rc=3 (M-7, l.260) ; UPDATE prestation_version → rc=3 (l.267) ; SELECT v_conditions_du_jour aux agrégats → rc=3 (M-15, l.423) ; UPDATE snapshot_marge → rc=3 ; INSERT personne aux agrégats → rc=3. Remise → rc=0, 29 OK. `suivi/V-012.txt` |
| V-013 | elevee | **Fermé** | `sonde2.mjs` rejouée : mode `auto_par_prestation_signee`, 1 prestation 50 % sur 2 postes → besoin reste `staffing`, `postes 1/2` (au lieu de `pourvu`). `projet.ts:219` appelle `gardePourvu`. `suivi/_sonde2.txt` |
| V-014 | elevee | **Partiel** | `temps.validation` gouverne (`projet.ts:471-474`) ; `droits.surcharge_restrictive` n'est plus lue. **Reste** : `doublon.personne.cles` défaut `[email, nom+prenom+naissance]` — seule la branche email existe (`identite.ts:22-31`), `CreatePerson` ne reçoit même pas de date de naissance ; `doublon.contact.cles` défaut `[email, nom+prenom+societe]` — seule email (`crm.ts:279-288`) ; `societe.passage_client.propagation` ne change que la liste écrite dans l'événement, aucune donnée (`projet.ts:197-211`). La clé reste écrite dans `liens.politiques`. |
| V-015 | elevee | **Partiel** | Transitions regroupées dans `server/src/cycle.ts:51-78`, lues par commandes et vues. **Reste** : la table est du code, pas un référentiel ; statut commercial toujours sur les **codes** (`cycle.ts:86-91` REQUALIF `prospect/client/ancien_client`, `crm.ts:54,90`, `projet.ts:195-196,208`) ; acteurs de politique → groupes en littéral (`cycle.ts:17-20`). P-139 ne cherche que les jetons de `CAT`/`GROUPE` entre guillemets doubles (`correctifs.test.ts:565-588`) et ne voit donc pas `'prospect'`/`"client"`. |
| V-016 | elevee | **Fermé** | `grep -rn "INSERT INTO ref_" server/src` → vide ; D-8 : `sorti` semé par 005:18-20. |
| V-017 | elevee | **Partiel** | Sonde : `CreatePerson civilite "M"`, `CreateNeed type/couverture inconnus`, `SetNeedPriority P_INVENTEE`, `CreateAction type inconnu` → GARDE ; `UpdateUnit` cycle → GARDE. **Reste** : `ManageRefs categorie:"categorie_inventee"` → `MUR erreur interne` (`_sonde1.txt` §6, `_sonde4.txt`). La cause est désormais tracée : `tentative_refusee` = « viole la contrainte ck_cat ». |
| V-018 | elevee | **Partiel** | `web/vite.config.ts` ne copie plus le bouchon. **Reste** : `web/src/contrat.ts:160` lit toujours `/bouchon/<Commande>.json`, `ListeBesoins.tsx:16` et `FicheBesoin.tsx:64,73` en dépendent ; aucune route serveur ne liste les besoins. P-061 → P-065 déclassées ⏳ lot 3 (`journal/PORTES.md:72-76`) : l'écran livré n'a plus de source. |
| V-019 | elevee | **Partiel** | Jetons lus (`test/ecran/tuyau.spec.ts:8-21`). Rejoué (copie, build web, Playwright) : jeton serveur `fond #111111` → P-004 tombe ✅. **Survivent** : titres en `#ff0000` écrits dans le CSS → P-004 et P-005 vertes ; fond de page `#330000` en dur dans le CSS → **P-004 (sombre) verte** (même sabotage que S9d du 1er audit). La porte lit les variables, pas la couleur calculée des éléments. `mutations/_resume_ecran.txt` |
| V-020 | elevee | **Fermé** | RES `UploadDocument` projet → DROIT ; société → DROIT ; sa propre personne → `ok:true`. `identite.ts:343-346`. `_sonde4.txt` |
| V-021 | elevee | **Partiel (accepté D-9)** | FK `fk_contact_statut`, `fk_coordonnee_type`, `fk_coordonnee_usage` en base ; CHECK `perimetre_type_code_check` maintenu par décision D-9 (arbitrage). `suivi/V-021_V-027.txt` |
| V-022 | elevee | **Ouvert** | `pg_hba_file_rules` : `local/host 127.0.0.1/::1 … trust` (6 lignes) ; `ava_serveur` sans mot de passe. Requalifié « poste de dev » par l'arbitrage, inchangé sur la machine. `suivi/V-022.txt` |
| V-023 | elevee | **Fermé** | `_ops/GRILLE_AUDIT.md:32` vise `ci.yml` ; B2/B4 renvoient au registre §E (l.47-48) ; A1 compte le plancher (l.30) ; F = 11 contrôles (l.126) ; B1 grep `server/src web/src` (l.45) ; C4 mesure toutes les relations (l.64, 81). |
| V-024 | moyenne | **Fermé** | `JEU_ESSAI.sql` chargé (rc=0, 21 001 personnes) : groupes 9, droits 88, compte_groupe 9 ; `CreateCompany` par IA → `ok:true`. ⚠️ voir candidat N-6. `suivi/V-024.txt` |
| V-025 | moyenne | **Fermé** | `test/contrat/sante.test.ts:4-5,23` lit `AVA_BASE_URL` ; P-002 verte contre 3201. |
| V-026 | moyenne | **Fermé** | `UpdateCandidate note_globale` écrit (`identite.ts:72,88`). Sabotage base (`note_dans_echelle()` en dur à 5) : P-134 **tombe** (assertions L7 restent rc=0). `suivi/V-026.txt` |
| V-027 | moyenne | **Fermé** | `has_table_privilege('ava_app','ava.tentative_refusee','INSERT')` = t (007 §3) ; `executer.ts:129-131` et `:196-198` journalisent l'échec (`console.error`). Sonde : un MUR écrit bien sa ligne `tentative_refusee`. |
| V-028 | moyenne | **Historique — tient** | `git log --diff-filter=M 3b59ca5..HEAD -- db/migrations/` → vide ; seule 007 ajoutée. `suivi/V-028.txt` |
| V-029 | moyenne | **Fermé** | MACHINES §1 porte `NeedSuspended/Resumed/Closed/Reopened (D-4)` (l.25-28), aucun `PositioningCreated`… ; L4:177 `mur_touche` indicatif ; MATRICE:157 D-7 SUP ; MACHINES:53 acteurs D-5 ; `doublon.contact.mode` une seule ligne (registre:152). `suivi/V-029.txt` |
| V-030 | moyenne | **Partiel** | Plus de message SQL au client (`index.ts:110-119`). **Reste** : `UpdateNeed id:"pas-un-uuid"` → **HTTP 500** `ERREUR erreur interne`, sans trace en `tentative_refusee` (`kernel.ts:172-181` ne convertit que 23503/23505/23514) ; tous les refus restent en HTTP 200. `suivi/V-030.txt` |
| V-031 | moyenne | **Fermé** (HEAD) | `git grep` motif `postgres:<ancienne valeur>@` → 0 fichier ; `docker-compose.yml:8` `${POSTGRES_PASSWORD:?…}`. La valeur reste dans l'historique (4 commits, non purgé). `suivi/V-031.txt` |
| V-032 | moyenne | **Partiel** | `ConvertCandidateToResource` rend `sortie.id` ; `PositionCandidate` → `CandidatePositioned`, `SignPrestation` → `PrestationSigned`, avec `evenements[]` de la transaction. **Reste** : `ArchiveObject` (chemin générique) rend `sortie = {}` sans id (`admin.ts:67`, `_sonde4.txt`). |
| V-033 | moyenne | **Fermé** | U1 → parent U2 (fille de U1) → `GARDE cycle dans l'arbre des unités`. `_sonde2.txt` |
| V-034 | moyenne | **Fermé** | Clôture au 2026-11-15, fin 2026-10-31 → `GARDE date de clôture après la fin`, état reste `signee`. `_sonde4.txt` |
| V-035 | moyenne | **Fermé** | 10 `CreateProject` simultanés → 10 références distinctes PRJ-0104 → 0113 ; `pg_advisory_xact_lock` (`projet.ts:5-12`). `_sonde4.txt` |
| V-036 | moyenne | **Partiel** | ordre/actif écrits (`7 false`) ; 2 `ManageGroups` identiques → 1 seul `GroupPermissionChanged`. **Défaut du correctif** : un simple renommage sans `actif` **réactive** la valeur (`7 false` → `7 true`, `admin.ts:123`). `_sonde4.txt` |
| V-037 | moyenne | **Partiel** | Société : délégué à `ArchiveCompany` → GARDE « des objets actifs ». **Reste** : `ArchiveObject` projet ouvert, ressource, besoin pourvu → `ok:true` sans aucune garde ni périmètre (`admin.ts:54-67`) ; L4:176 « GARDE selon l'objet ». `_sonde4.txt` |
| V-038 | moyenne | **Fermé** | Au démarrage banc, `res@ava.test` → personne + 1 profil ressource (`index.ts:69-106`) ; P-155. ⚠️ régresse après le jeu d'essai : candidat N-6. |
| V-039 | moyenne | **Partiel** | Captures Linux du tuyau versionnées ; `besoin.spec.ts` ignoré (`playwright.config.ts:6`). **La CI ne peut toujours pas passer** : `ci.yml` n'a ni service Postgres ni `POSTGRES_PASSWORD` ; sur Linux `make.sh:36-61` prend la branche docker et sort 1 (« POSTGRES_PASSWORD requis ») → cliquet case 1 KO. Lecture seule, CI non exécutée. |
| V-040 | moyenne | **Partiel** | Trailer `Role:` sur 44/52 commits depuis 3b59ca5 (les 8 sans sont antérieurs au hook) ; `.githooks/commit-msg`. **Reste** : `git config core.hooksPath` vide dans un clone neuf, aucun script ne le pose (`git grep hooksPath -- outils .github` → 0) ; 5 commits `greffe` écrivent `server/src` ET `test/contrat` dans le même commit. `suivi/V-040.txt` |
| V-041 | bonne | **Tient** | Mutation S3a (CreateCompany écrit puis lève) → `ERREUR`, 0 société persistée. ⚠️ S3b (ROLLBACK → COMMIT sur refus) : 120/120 portes vertes, 1 société persistée — aucune porte exécutable ne le voit. `mutations/S3a_*.txt`, `S3b_*.txt` |
| V-042 | bonne | **Tient** | DISABLE `tg_m10`, `tg_m7`, `tg_m4_m14`, `tg_m6`, `tg_m12`(besoin, unite) → rc=3. ⚠️ `tg_m12` sur `contact` et sur `projet` désactivés → **rc=0** : candidat N-7. `suivi/V-042.txt` |
| V-043 | bonne | **Tient** | Matrice (✓/S, lignes A·B dépliées) : 86 cases = 86 lignes seed, 0 écart dans les deux sens. `suivi/V-043.txt` |
| V-044 | bonne | **Tient** | 11 interpolations `${…}` en SQL : colonnes/tables tirées de constantes (`admin.ts:5-24`, `besoin.ts:251-254`, `cycle.ts`), `ManageRefs` regex + `pg_class` (`admin.ts:107-113`) ; 0 `dangerouslySetInnerHTML`. |
| V-045 | bonne | **Tient** | Sortie RH de `ConvertCandidateToResource` sans clé `cout*` ; `UpdateResource cout_reference` → `GARDE le coût se change par UpdateResourceCost`. `_sonde4.txt` |
| V-046 | bonne | **Tient** | STAF `CreatePrestation etat:signee` → DROIT (`_sonde1.txt` §2) ; snapshot md5 identique avant/après `AdjustTimesheetAfterClose` (`_sonde4.txt`). O-2 a maintenant une porte (P-126 tombe sous S4c). |
| V-047 | bonne | **Tient** | 173 politiques (172 §C + `ui.theme.personnalise`), 0 clé manquante ; `valeur<>defaut` = 0 ; 40 `ref_*` = registre §E ; `git diff origin/main HEAD -- _ops/` → 0 fichier. `suivi/V-047.txt` |

## Compte

| Verdict | Nombre | V |
|---|---|---|
| Fermé | **22** | 001 002 005 007 008 009 010 012 013 016 020 023 024 025 026 027 029 031 033 034 035 038 |
| Partiel | **15** | 004 006 011 014 015 017 018 019 021 030 032 036 037 039 040 |
| Ouvert | **2** | 003 022 |
| Historique (tient) | 1 | 028 |
| Bonne — tient | 7 | 041 → 047 (aucune régression) |

**Critiques encore ouverts ou partiels : V-003 (Ouvert), V-004, V-006, V-011 (Partiels).**

## Constats neufs candidats

| # | Défaut | Cible | Gravité | Preuve | Reproduire | Correction |
|---|---|---|---|---|---|---|
| N-1 | Hors banc, l'en-tête `x-ava-session` accepte l'UUID d'un compte comme session : aucun secret, et les UUID du seed sont fixes | CODE | critique | `executer.ts:72-83` ; `suivi/V-003.txt` §3 et §5 | serveur sans `AVA_MODE`, `curl -H 'x-ava-session: 00000000-0000-4000-8000-000000000108' …/SetPolicy` → 200 ok | Une session est un jeton opaque émis après authentification, stocké haché, avec une expiration. Jamais l'id du compte. |
| N-2 | Les portes de contrat lisent et écrivent la base « ava » dès que `DATABASE_URL` manque. `make.sh` ne l'exporte jamais, et `correctifs.test.ts` écrit « ava » en dur. Résultat : `AVA_DB=…` ne protège pas la base du voisin, et les portes relisent une autre base que celle du serveur | CODE (banc) | elevee | `test/contrat/db.ts:8` ; `outils/make.sh:20,36-46,243` ; `correctifs.test.ts:83,137` | `grep -n "5432/ava" test/contrat/*.ts` ; `grep -n DATABASE_URL outils/make.sh` | `make.sh` exporte `DATABASE_URL` depuis `AVA_URL_MIGRATION`, et `db.ts` lève une erreur si elle manque, au lieu de prendre un défaut. |
| N-3 | La CI ne peut pas passer : sans service Postgres ni `POSTGRES_PASSWORD`, `make.sh` sort 1 sur Linux et la case 1 est toujours KO. En plus, `ava_serveur` n'a pas de mot de passe face à l'authentification scram du conteneur | CODE | elevee | `.github/workflows/ci.yml` (aucun `services:`/`env:`) ; `outils/make.sh:56-61` | lire les deux fichiers ; un run CI sur `lot-2` | Service `postgres:16` dans `ci.yml` + secrets, ou un chemin « local » Linux, et un mot de passe CI pour `ava_serveur`. |
| N-4 | Les cases 3 et 9 du cliquet ne protègent que les 5 portes servies sur `main`. On peut retirer de `PORTES.md` des portes ✅ propres à `lot-2` (P-066 → P-071…) sans qu'aucune case ne bouge, et la case 1 cesse alors de les exiger | CODE (banc) | moyenne | `suivi/V-009.txt` §B : cases 3 et 9 OK, P-066 absent de `non_jouées` | retirer les lignes P-066..P-071 d'une copie, lancer `cliquet_audit.sh` | Comparer aussi à la dernière version de `PORTES.md` de la branche (ou au commit de base de la PR), pas seulement à `main`. |
| N-5 | P-133 « temps.plafond_jour : deux résultats » ne joue jamais `RecordTimesheet` : elle règle la politique et la relit. Fausse porte ✅ | CODE (banc) | elevee | `test/contrat/politiques.test.ts:74-87` ; mutation S5d → 120/120 | `bash scripts/banc_mut.sh S5d_temps.plafond_jour` | Saisir 1,5 j sous `alerte` (écrit + alerte) puis sous `refus` (GARDE, rien d'écrit). |
| N-6 | Après `_ops/JEU_ESSAI.sql`, le compte RES garde sa personne mais perd son profil ressource. `assurerBancRes` ne le recrée jamais, même au redémarrage, parce qu'il sort dès que `personne_id` est posé | CODE | moyenne | `suivi/V-024.txt` (`profils_ressource=0` avant et après redémarrage) ; `server/src/index.ts:78` | charger le jeu, redémarrer le serveur banc, compter `profil_ressource` du compte RES | Tester l'existence du profil ressource, pas celle de `personne_id` ; ou semer le lien RES dans le jeu d'essai. |
| N-7 | Les assertions L7 ne couvrent pas M-12 sur `contact` ni sur `projet` : leurs triggers désactivés laissent rc=0 | BRAIN (assertions) | moyenne | `suivi/V-042.txt` | `ALTER TABLE ava.projet DISABLE TRIGGER tg_m12` puis `psql -f test/SPEC_ASSERTIONS_L7.sql > f` → rc=0 | Une assertion M-12 par table porteuse du trigger. |
| N-8 | `ManageRefs` sans `actif` réactive une valeur désactivée | CODE | moyenne | `admin.ts:123` ; `_sonde4.txt` (`7 false` → `7 true`) | désactiver puis renommer une valeur | `actif` absent = inchangé (`COALESCE`). |
| N-9 | `SetPolicy` accepte n'importe quelle valeur pour une politique sans `valeurs_possibles` (une liste JSON réglée à « pas du json ») ; `parseListe` dégrade ensuite en silence | CODE | moyenne | `_sonde1.txt` §5, `_sonde4.txt` ; `admin.ts:80-87` | `SetPolicy candidat.complete.champs_requis "pas du json"` → ok | Valider selon le type déclaré de la politique (liste, nombre, JSON) avant l'UPDATE. |

## Angles morts

| Non vérifié | Pourquoi |
|---|---|
| `correctifs.test.ts` (P-066 → P-156) jamais lancé | interdit : il démarre des serveurs sur la base « ava » (`:83`, `:137`). Remplacé par des sondes directes. Les mutations ne comptent que les 4 suites exécutables : S6 (pourvu sans garde) survit dans ces 4 suites, et P-137 la verrait probablement (lu, non joué) |
| `make.sh test` et `cliquet.sh` de bout en bout | interdit. Cliquet rejoué dans une copie, avec un journal injecté à la place de make |
| CI GitHub | non exécutée ; V-039 et N-3 sont établis en lisant `ci.yml` et `make.sh` |
| Portes geste et écrans Besoins | `besoin.spec.ts` est ignoré (⏳ lot 3) ; seul le tuyau P-004/P-005 a été rejoué |
| Périmètre : couverture exhaustive | 20 commandes sondées sur CAS, pas les 55 ; `RecordTimesheet` et `ClosePrestation` ont refusé en ETAT avant le contrôle de périmètre |
| V-022, V-040 | réglages du poste et discipline humaine : constat seulement |
| Artefact Windows | sous Git Bash, `psql -f … >/dev/null` rend rc=3 même quand tout passe. Toutes les mesures de rc ont été prises avec une sortie vers un fichier |
| Rôles globaux | 007 fait `ALTER ROLE ava_serveur …` à chaque construction de base : mes reconstructions ont remis les attributs du rôle **pour tout le cluster**, y compris pour l'autre auditeur (`ava_audit2`), s'il sabote ce rôle |
